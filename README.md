# 目标
使用cpp + clangd复现`Attention is all you need`

# 平台
MacOS 13+
# Requirements
```shell
brew install xtl
brew install xsensor
brew install xsmid
brew install sqlitecpp
brew install fmt
brew install spdlog
```